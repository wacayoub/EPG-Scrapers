name: Probe official Arab media

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/probe-official-arab-media.yml"
      - "tools/official_arab_media_probe.py"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check --no-cache-dir requests beautifulsoup4
      - name: Probe official/local/government Arab media
        run: python tools/official_arab_media_probe.py
      - name: Show discovery matrix
        run: cat reports/official-arab-media-discovery.md
